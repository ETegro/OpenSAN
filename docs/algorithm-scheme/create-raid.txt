graph { flow: south }

( Создание RAID

[] -> [ есть ли PV? ]

[ есть ли PV? ] -- ДА --> [ есть ли VG? ]
[ есть ли PV? ] -- НЕТ --> [ создать RAID ]

[ есть ли VG? ] -- ДА --> {flow: back} [ остановить VG ] --> [ создать RAID ]
[ есть ли VG? ] -- НЕТ --> [ создать RAID ]

[ создать RAID ] --> [ prepare( созданный RAID ) ]

)
