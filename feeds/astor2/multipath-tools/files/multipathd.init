#!/bin/sh /etc/rc.common
START=55

PID_FILE=/var/run/multipathd.pid
MULTIPATHD=/sbin/multipathd

start()
{
	$MULTIPATHD
	echo $! > $PID_FILE
}

stop()
{
	kill -9 `cat $PID_FILE`
}

restart()
{
	stop
	start
}
